# Demo mode: "simulation" (default) or "live"
DEMO_MODE=simulation

# Live mode only — agent Solana private key (base58, devnet funded)
AGENT_SOLANA_KEY=

# Live mode only — merchant wallet address (receives payments)
MERCHANT_ADDRESS=

# Live mode only — PincerPay API key (from dashboard)
PINCERPAY_API_KEY=

# Live mode only — facilitator URL
FACILITATOR_URL=http://localhost:4402
